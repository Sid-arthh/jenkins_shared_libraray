@Library("my-shared-lib") _
pipeline {
    agent any
    environment {
        ECS_CLUSTER_NAME = 'SHARED_LIB'
        ECS_SERVICE_NAME = 'SERVICE1'
    }
    stages {
        stage('Build and Deploy') {
            steps {
                script {
                    // Call the wrapper function from the shared library
                    deployToECSWrapper()
                }
            }
        }
    }
}
